import {describe,expect,it} from "vitest";import en from "../../messages/en.json";import ar from "../../messages/ar.json";
describe("message catalogs",()=>{it("keep the same top-level namespaces",()=>{expect(Object.keys(ar).sort()).toEqual(Object.keys(en).sort())});it("contain Arabic calendar actions",()=>{expect(ar.Calendar.markComplete).toBe("وضع علامة مكتمل")});it("use Areas and Tasks terminology in both locales",()=>{expect(en.Nav.areas).toBe("Areas");expect(en.FocusManager).toMatchObject({area:"Area",subtask:"Task"});expect(ar.Nav.areas).toBe("المجالات");expect(ar.FocusManager).toMatchObject({area:"مجال",subtask:"مهمة"})})});
